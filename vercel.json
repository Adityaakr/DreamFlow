{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/stream.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/.*|health).*)",
      "destination": "/index.html"
    }
  ]
}
